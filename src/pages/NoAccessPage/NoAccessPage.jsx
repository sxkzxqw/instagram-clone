import React from 'react';

const NoAccessPage = () => {
    return (
        <div style={{display: 'flex', alignItems:'center', textAlign:'center', justifyContent:'center', height: '100vh'}}>
            No access to open this page
        </div>
    );
};

export default NoAccessPage;